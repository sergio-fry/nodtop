<p>В рейтинге могут принять участие только сайты-сторонники <a href="http://rusnod.ru" target="_blank">НОД</a>.</p>

<table class="table table-striped table-hover sites-table">
  <thead>
    <tr>
      <th width="100%">Сайт</th>
      <th></th>
      <th>Рейтинг</th>
      <th>%&nbsp;НОД</th>
    </tr>
  </thead>

  <tbody>
  <% @sites.popular.each do |site| %>
    <tr class="site" data-rating="<%= site.rating %>">
      <td><%= link_to (site.title.presence || site.domain), site.url %></td>
      <td><%= link_to content_tag(:span, nil, :class => "glyphicon glyphicon-cog", :title => "Получить код для вставки"), site %></td>
      <td><%= '%.2f' % site.rating %></td>
      <td class="share"></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Добавить свой сайт', new_site_path, :class => "btn btn-primary" %>

