<p>Рейтинг сайтов, сторонников <a href="http://rusnod.ru" target="_blank">национально-освободительного движения</a>.</p>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th width="100%">Сайт</th>
      <th>Рейтинг</th>
    </tr>
  </thead>

  <tbody>
  <% @sites.popular.each do |site| %>
    <tr>
    <td><%= link_to (site.title.presence || site.domain), site.url %></td>
    <td><%= site.rating %></td>
  </tr>
<% end %>
  </tbody>
</table>

<br>

<%= link_to 'Добавить свой сайт', new_site_path, :class => "btn btn-primary" %>

